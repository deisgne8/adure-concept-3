# Adure Concept 3 - Abhi Dev

Fresh Next.js and Tailwind workspace for the `abhi-dev` branch.

## Branch Rule

- Work only on the `abhi-dev` branch.
- Do not modify, reset, overwrite, or push changes to `main`.
- Treat `main` as untouched source history.

## Scripts

```bash
npm run dev
npm run build
```
